CURRENT_DATE=$(date +"%Y-%m-%d")

export DEV_DIR='/home/bernardo/dev'
export ZK_NOTEBOOK_DIR="$DEV_DIR/borbsidian"
export VETTA_ORG_DIR="$DEV_DIR/vettadigital/"
export SMS_ORG_DIR="$DEV_DIR/sms-digital/"
export VETTA_PIPELINE_DIR="$VETTA_ORG_DIR/Vetta/platform-vetta-automation"
export SMS_PIPELINE_DIR="$SMS_ORG_DIR/Tech\ SMS/platform-sms-automation"

# Directories I usually go to
alias dev='cd $DEV_DIR'
alias vet='cd $VETTA_ORG_DIR'
alias sms='cd $SMS_ORG_DIR'
alias pvetta='cd $VETTA_PIPELINE_DIR'
alias psms="cd ${SMS_PIPELINE_DIR}"

# Zettelkasten
alias ob='nvim /mnt/g/Meu\ Drive/Obsidian/borbsidian/periodic/daily/${CURRENT_DATE}.md'

# Git
alias glog="git log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(auto)%d%C(reset)' --all"

# Kubernetes
alias ku="kubectl"

alias e="explorer.exe"

docker() {
  if docker_path=$(command -v docker); then
    "$docker_path" "$@"
  else
    podman "$@"
  fi
}
